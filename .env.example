# System Admin Bootstrap
# Set this to automatically elevate a user to system-admin role on first login
# System admins can manage namespaces and users across all namespaces
SYSTEM_ADMIN_EMAIL=admin@example.com

# Supabase Configuration
# After running `npm run supabase:start`, copy the API URL and keys from the output
# Local development uses these values (Supabase CLI provides default keys)
NEXT_PUBLIC_SUPABASE_URL=http://localhost:54321
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=<from supabase start output>
SUPABASE_SECRET_KEY=<from supabase start output>

# Note: Variable names match Supabase's current terminology
# "publishable" = safe to expose client-side (anon key in dashboard)
# "secret" = server-only (service_role key in dashboard)

# AI Code Analysis (Optional)
# Get a free API key from https://aistudio.google.com/app/apikey
# Used for the AI Walkthrough feature that analyzes student code submissions
GEMINI_API_KEY=

# Rate Limiting (Upstash Redis REST API)
# REQUIRED in production to prevent API abuse
# Create a free Redis database at https://upstash.com
# IMPORTANT: Use the REST API URL (https://...), NOT the Redis protocol URL (redis://)
# Find these in your Upstash console under "REST API" section
# In development, rate limiting is disabled if these are not set
UPSTASH_REDIS_REST_URL=https://your-database.upstash.io
UPSTASH_REDIS_REST_TOKEN=
