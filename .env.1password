# 1Password secret references for coding-tool
#
# Format: VARIABLE_NAME=[1password-secret-reference]
#
# This file is processed by setup-secrets.sh on container start.
# envsubst expands ${OP_VAULT}, then op inject resolves the references.
# NOTE: Do not put secret references in comments — op inject tries to resolve them all.
#
# GH_TOKEN is handled separately in setup.sh (file-based, like OP_SERVICE_ACCOUNT_TOKEN).

# Supabase (populated from local supabase start output)
NEXT_PUBLIC_SUPABASE_URL=http://localhost:54321
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=${SUPABASE_ANON_KEY}
SUPABASE_SECRET_KEY=${SUPABASE_SERVICE_ROLE_KEY}

# Shared secrets from 1Password
SYSTEM_ADMIN_EMAIL=op://${OP_VAULT}/secrets/system-admin-email
GEMINI_API_KEY=op://${OP_VAULT}/secrets/gemini-api-key

# Rate Limiting (optional for local dev)
# In development, rate limiting is disabled if these are not set
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
