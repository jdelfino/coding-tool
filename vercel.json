{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm run build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["iad1"],
  "ignoreCommand": "if [ \"$VERCEL_GIT_COMMIT_REF\" = \"beads-sync\" ]; then exit 0; else exit 1; fi"
}
